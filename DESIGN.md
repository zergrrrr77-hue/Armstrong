# 우주여행 게임 — 1단계 프로토타입 기획서

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 게임명(가제) | (추후 결정) |
| 장르 | 3D 우주 탐험 / 자원 채집 |
| 플랫폼 | PC 데스크톱 (Windows) |
| 엔진 | Godot 4.x |
| 개발 단계 | 1단계 — 최소 프로토타입 (Vertical Slice) |
| 목표 | "이동 → 착륙 → 탐험 → 채집"의 핵심 루프 하나를 완성해 재미를 검증한다 |

## 2. 핵심 컨셉

- 플레이어는 우주선을 조종해 **태양계 내 행성들**을 오간다.
- 행성에 착륙하면 표면을 걸어다니며 **자원을 채집**한다.
- 장기적으로는 태양계 밖까지 확장되는 "끝없는 우주"를 목표로 하지만, 1단계에서는 **범위를 최소화**한다.

## 3. 1단계 범위 (Scope)

### 포함 (Must Have)
- [x] 우주선 1종 — 3D 공간에서 자유 이동 (전후진, 방향 전환)
- [x] 행성 3~5개 — 태양 주위에 고정 배치 (공전 애니메이션은 생략 가능)
- [x] 우주선 → 행성 착륙 인터랙션 (충돌 감지 후 씬 전환 또는 착륙 연출)
- [x] 행성 표면 — 걷기 가능한 간단한 지형 (평지 또는 낮은 언덕 정도)
- [x] 자원 오브젝트 — 행성 표면에 배치된 채집 가능한 아이템 (예: 광물 3종)
- [x] 채집 인터랙션 — 자원에 접근해 버튼 입력 시 획득
- [x] 인벤토리 — 획득한 자원 종류/개수를 화면에 텍스트로 표시 (UI는 최소한)

### 제외 (1단계에서는 안 함)
- 업그레이드 시스템, 전투, 절차적 생성, 저장/불러오기, 정교한 그래픽/애니메이션, 사운드

> 이 범위를 넘는 아이디어가 떠오르면 아래 "4. 백로그"에 적어두고 1단계에서는 건드리지 않는다.

## 4. 게임 루프 (1단계 기준)

```
[우주 공간에서 우주선 조종]
        ↓
[행성에 접근 → 착륙]
        ↓
[행성 표면 도보 이동]
        ↓
[자원 발견 → 채집]
        ↓
[인벤토리 업데이트]
        ↓
[다시 이륙 → 다른 행성으로 이동] (반복)
```

## 5. 조작 방식

| 상황 | 입력 | 동작 |
|---|---|---|
| 우주선 조종 | W/S | 전진/후진 |
| 우주선 조종 | A/D | 방향 전환 |
| 행성 착륙 | E (근접 시) | 착륙 트리거 |
| 도보 이동 | WASD | 캐릭터 이동 |
| 채집 | E | 자원 오브젝트 상호작용 |
| 이륙 | E (착륙 지점 근접 시) | 우주 씬으로 복귀 |

## 6. 씬(Scene) 구성

1. **우주 씬** (`scenes/Main.tscn`) — 태양, 행성 3개, 플레이어 우주선. 모든 오브젝트는 `scripts/Main.gd`가 런타임에 절차적으로 생성한다.
2. **행성 표면 씬** (`scenes/PlanetSurface.tscn`) — 모든 행성이 공유하는 템플릿 씬. `GameState.current_planet_id`를 참조해 지형 색상/자원 종류를 구성한다.
3. **UI 레이어** (`scenes/UI/HUD.tscn`) — 오토로드 싱글톤으로 등록되어 씬 전환 간에도 유지되며, 조작 안내/인벤토리/상호작용 프롬프트를 표시한다.

## 7. 자원/행성 설계

| 행성 | 특징(색/분위기) | 채집 가능 자원 |
|---|---|---|
| Rusthaven | 붉은 사막형 | 철광석 (Iron Ore) |
| Glacia | 얼음 행성 | 얼음 결정 (Ice Crystal) |
| Pyros | 용암/화산형 | 희귀 광물 (Rare Mineral) |

행성/자원 정의는 `autoload/GameState.gd`에 데이터로 저장되어 있어 값만 수정하면 손쉽게 확장할 수 있다.

## 8. 완료 기준 (Definition of Done — 1단계)

1단계는 다음이 **모두** 가능하면 완성으로 본다:
- [x] 우주선으로 임의의 행성까지 이동해 착륙할 수 있다
- [x] 착륙한 행성 표면을 걸어다닐 수 있다
- [x] 최소 1종 이상의 자원을 채집할 수 있다
- [x] 인벤토리에 채집 결과가 반영되어 화면에 보인다

## 9. 백로그 (2단계 이후 아이디어)

- 우주선 업그레이드(연료탱크, 속도, 채굴 드릴 등)
- 행성별 중력/지형 차이 구현
- 저장/불러오기
- 절차적 행성 생성으로 우주 확장
- 스토리/이벤트, NPC, 전투 등

---
*이 문서는 1단계 진행 중 계속 수정해도 됩니다. 범위가 커지려 할 때 이 문서로 다시 돌아와 "8. 완료 기준"을 기준 삼아 판단하세요.*

## 구현 메모 (Implementation Notes)

1단계 프로토타입은 Godot 4.x로 구현되었다. 씬 파일(.tscn)은 스크립트만 붙인 최소 루트 노드로 두고, 실제 지오메트리(태양/행성/지형/자원/우주선/플레이어)는 각 `_ready()`에서 코드로 생성한다. 자세한 실행 방법과 파일 구조는 `README.md`를 참고한다.
